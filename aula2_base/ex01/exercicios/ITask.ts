/* Exercício 1 — Criar interface Tarefa */

/* Cria uma interface chamada Tarefa que defina a estrutura de um objeto de tarefa.
- A interface deve conter: id (number), titulo (string) e concluida (boolean).
- Lembre-se que a interface apenas define o "contrato", não cria objetos reais ainda. */
export default interface ITask {
  id: number;
  title: string;
  completed: boolean;
}




